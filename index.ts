interface NCharsTable {}

function getNCharsFromText(text: string, n: number) {
  // ...
}

function distanceBetweenTables() {}

export default function (text: string, tables: NCharsTable[]) {
  // for each table in tables:
  // get distance between getNCharsFromText(text) and table
  // find the closest one
  // return
}
